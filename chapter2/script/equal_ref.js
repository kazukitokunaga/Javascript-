console.log(1 == true); // 結果：true

var data1 = ['JavaScript', 'Ajax', 'ASP.NET'];
var data2 = ['JavaScript', 'Ajax', 'ASP.NET'];
console.log(data == data2); // 結果：false

/*
 * 参照型では、その参照値（メモリ上のアドレス）が格納されるので、
 * 参照値（メモリ上のアドレス）が等しい場合にのみtrueが返される。
 */
